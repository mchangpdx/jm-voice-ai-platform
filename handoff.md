1. 오늘 한 일: N2 dietary inference (4 vertical yaml 활용, 39 row backfill); 매장 자동 등록 P0 6건 (A1 biz_hours UI / A2 JWT auto agency+owner / A3 persist / A4 sidebar refresh CustomEvent / A6 DEFAULT_PERSONAS 10 vertical + JM Taco backfill); N1 recent_orders tool 신규 (cross-call cancel/modify entry, 30분 lookback, single/multi-match render); Greeting Bug 1 fix (CRM block에 store_name 주입 + persona 동적 추출 + ABSOLUTE RULE first-sentence store name); recent_orders trigger 키워드 확장 + cancel_order PRECONDITION 가드; Bug 2 PSTN noise race condition fix (interrupt_response toggle + buffer clear + 300ms wait + greeting_done one-shot latch). 7 atomic commits (29e4fc4 → 5fef4e1). 7 라이브 검증 통화 모두 통과 (그리팅 6/6 완벽, recent_orders EN+KO 양쪽 fire, cancel_order chain 완료, modify_order recap, CRM fast-path)
2. 다음 세션에서 가장 먼저 할 일: ⭐ session_resume_2026-05-17.md 먼저 read. P0 sprint — ① Backend production hosting (Fly.io, 8-12h), ② SumUp PG 어댑터 신규 (P0, 1차 결제 게이트웨이, 2-3일), ③ Staging environment 셋업 + Feature flags (Supabase 테이블 기반, 1-2주), ④ CI/CD 파이프라인 (GitHub Actions, 4-6h), ⑤ EU AI Act 준수 작업 (2026-08-02 deadline). POS 전략 변경: Loyverse + SumUp 1차 (QSR/SMB), Quantic은 2차 FSR 진입 시점 보류. Maverick 작업도 2차 보류.
3. 절대 하지 말 것: 통화 중 backend 파일 수정 (uvicorn reload가 WebSocketDisconnect로 통화 끊음); Loyverse webhook DELETE/POST (cache 오류); recall_order in-call snapshot 제약 해제 (Phase 5 #25 invariant, cross-call은 recent_orders 사용); agency_id frontend override (JWT가 source of truth)
4. 참고: 7 commits 5fef4e1..29e4fc4, +1,488/-65, 150+ unit tests pass; live calls CAea87a1a8 (N1 cross-call cancel 완료), CAa83bff0f (한국어 cross-call), CAea4a9a6e (Bug 2 잘림 pre-fix 비교), CA438ad0 (recent_orders 미fire pre-trigger-fix 비교); 메모리 [[session_resume_2026-05-17]] / [[phase_flow_gate_2026-05-16]] / [[n1_n2_diagnosis_2026-05-16]]
