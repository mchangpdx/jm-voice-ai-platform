"""Beauty MVP service-vertical skills (Phase 3).
(Beauty MVP service-vertical 스킬 — Phase 3)

Mirrors app.skills.scheduler for reservations but targets the
`appointments` table used by service-kind verticals (beauty / future
auto_repair / home_services).

See backend/app/templates/_base/spec.md §3 Layer 6 (scheduler) for the
slot-model side and Beauty MVP plan §Phase 3 for the tool contracts.
"""
