# Internal admin endpoints — onboarding/migration tooling.
# (내부 관리 API — onboarding/migration 도구)
#
# These endpoints are NOT exposed in the public OpenAPI and require service
# role auth in production. For Phase 7-A onboarding work the gate is just
# a shared secret in the X-Admin-Token header.
